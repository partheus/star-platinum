{
  "name": "parth-eleventy-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"eleventy --serve\" \"sass --watch src/scss:css\"",
    "build": "eleventy",
    "sass": "sass src/scss:css"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "concurrently": "^8.2.2",
    "sass": "^1.75.0"
  }
}
