---
layout: layouts/layout.njk
templateOverrideEngine: njk, md
eleventyComputed:
  pageTitle: "{{ project.title }}"
  pageSubtitle: "{{ project.year }}"
---
{% block content %}
    <article class="project-section">
        <img src="{{ project.image }}" alt="{{ project.title }}">
        <div>
            <ul class="project-tech-stack">
                {% for tech in project.tech %}
                    <li>{{ tech }}</li>
                {% endfor %}
            </ul>
            <p class="">More info: {{ project.description }}</p>
            <a href="{{ project.liveSite }}" class="button button--secondary">View Live Site</a>
        </div>
    </article>
    <br>
    <a href="/projects" class="button button--primary">Back to Projects</a>
{% endblock %}


